{
  "name": "angular",
  "version": "1.0.0",
  "description": "AngularJS",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "npm_install": "npm install",
    "bower_install": "bower install",
    "start": "http-server -a localhost -p 80 -c-1 ./app"
  }
}
